<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="ASA_Save_Inspector.Pages.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ASA_Save_Inspector.Pages"
    xmlns:asi="using:ASA_Save_Inspector"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <StackPanel Margin="4">
            <TextBlock Text="{x:Bind asi:ASILang.Get('WelcomeToASI'), Mode=OneWay}" Style="{ThemeResource TitleLargeTextBlockStyle}" Margin="42,0,0,0"/>
            <StackPanel Orientation="Vertical" Margin="10,15,0,0">
                <TextBlock FontSize="18" TextWrapping="WrapWholeWords">
                            <Run FontWeight="Bold" Text="{x:Bind asi:ASILang.Get('QuickStart'), Mode=OneWay}"/>
                            <LineBreak/>
                            <Run Text="{x:Bind asi:ASILang.Get('QuickStart_DescriptionLine1'), Mode=OneWay}"/>
                            <LineBreak/>
                            <Run Text="{x:Bind asi:ASILang.Get('QuickStart_DescriptionLine2'), Mode=OneWay}"/>
                            <LineBreak/>
                            <Run Text="{x:Bind asi:ASILang.Get('QuickStart_DescriptionLine3'), Mode=OneWay}"/>
                            <LineBreak/>
                            <Run Text="{x:Bind asi:ASILang.Get('QuickStart_DescriptionLine4'), Mode=OneWay}"/>
                </TextBlock>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
